# Temporary dispatch handle only. The workflow name is already registered;
# this branch must not restore the integrated/native matrix jobs.
name: Integrated isolated candidate
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qik-broker-pg:
    uses: ./.github/workflows/qik-broker-pg-qual.yml
