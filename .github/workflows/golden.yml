name: Golden regression suite

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    if: ${{ github.ref != 'refs/heads/codex/mip-backend-consolidation-20260920' && github.head_ref != 'codex/mip-backend-consolidation-20260920' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24]
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: ${{ matrix.node }}
      # G-ALG-0 added a real runtime dependency (graphology); the suite no
      # longer runs on zero node_modules, so npm ci is required first.
      - run: npm ci
      - run: npm test
      - run: npm run build
      - name: Dependency audit (triage report)
        if: matrix.node == 24
        continue-on-error: true
        run: npm audit --json

  intake-reader:
    name: J1 native preservation recovery
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/codex/mip-backend-consolidation-20260920' && github.run_attempt == 1 && github.event.head_commit.message == 'J1 isolated execution 9' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Prepare pinned disposable dependencies
        shell: bash
        run: |
          set -euo pipefail
          git rev-parse HEAD HEAD^{tree} HEAD:src HEAD:tests HEAD:package-lock.json
          ! grep -q extraheader .git/config
          docker pull postgres@sha256:00bc86618629af00d2937fdc5a5d63db3ff8450acf52f0636ec813c7f4902929
          docker build -f verifier/integrated/native-path.Dockerfile -t mip-j1-python verifier/integrated
      - name: Execute J1 only without external network
        shell: bash
        run: |
          set -euo pipefail
          docker volume create mip-j1-socket
          docker run -d --name mip-j1-pg --network none --memory 512m --pids-limit 128 \
            --security-opt no-new-privileges:true \
            --tmpfs /var/lib/postgresql/data:rw,noexec,nosuid,size=256m \
            --mount type=volume,source=mip-j1-socket,target=/var/run/postgresql \
            -e POSTGRES_PASSWORD=mip-j1-disposable-only \
            postgres@sha256:00bc86618629af00d2937fdc5a5d63db3ff8450acf52f0636ec813c7f4902929
          stable=0
          for attempt in $(seq 1 60); do
            if docker exec mip-j1-pg pg_isready -U postgres >/dev/null 2>&1; then stable=$((stable + 1)); else stable=0; fi
            if [ "$stable" -ge 5 ]; then break; fi
            test "$attempt" -lt 60
            sleep 1
          done
          test "$(docker inspect --format '{{.HostConfig.NetworkMode}}' mip-j1-pg)" = none
          test -z "$(docker port mip-j1-pg)"
          docker run --name mip-j1-test --network none --read-only --cap-drop ALL \
            --security-opt no-new-privileges:true --user 65534:65534 --memory 256m --pids-limit 64 \
            --tmpfs /tmp:rw,noexec,nosuid,size=32m \
            --mount type=volume,source=mip-j1-socket,target=/var/run/postgresql,readonly \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            -e MIP_J1_ISOLATION=network-none-socket-only \
            mip-j1-python /workspace/verifier/jfn-retirement/recovery_qualification.py
      - name: Preserve diagnostics and remove disposable runtime
        if: always()
        shell: bash
        run: |
          docker logs mip-j1-test 2>&1 || true
          docker inspect mip-j1-pg mip-j1-test --format 'BOUNDARY {{.Name}} network={{.HostConfig.NetworkMode}} memory={{.HostConfig.Memory}} ports={{json .HostConfig.PortBindings}} exit={{.State.ExitCode}}' || true
          docker rm -f -v mip-j1-test mip-j1-pg >/dev/null 2>&1 || true
          docker volume rm mip-j1-socket >/dev/null 2>&1 || true
          test -z "$(docker ps -aq --filter name=^/mip-j1-pg$ --filter name=^/mip-j1-test$)"
          ! docker volume inspect mip-j1-socket >/dev/null 2>&1
          printf '%s\n' 'J1_DISPOSABLE_CLEANUP_PASS'
