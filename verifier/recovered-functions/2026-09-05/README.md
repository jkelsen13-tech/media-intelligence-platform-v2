# Recovered function snapshots (2026-09-05)

These files are historical deployment snapshots from the original MIP project and the later Manus corpus. They are **not** the active Edge Function directory.

Do not upload them wholesale to the current website backend. The recovered Arc and Source Comparison membership models keep automatic approval disabled. Passing fixtures are not calibration approval.

Active integration goes through the destination evidence pipeline (`mip_pipeline_v1`) and `scripts/algorithmEvidenceAdapter.mjs`.
