# Frozen PR161 integrated-boundary review evidence

Candidate: 208e160684407e5856ad04209cbe45ba8e05b865
Candidate tree: 8de1fcaaea442107247b25f1528d82359675071c
Base: 67e7a962b07ceaec75e06442ef770843d126e3ae
Test merge: 1a2c46d36084c2380d8c3d42ace77d9ad1d9c0d5; its tree equals the candidate tree.

This branch stores implementation evidence, not an independent review verdict or controller authorization. Review the frozen candidate and its installed dependencies, not this evidence commit as the code candidate. Do not merge, deploy, provision credentials or expand disclosure based on this packet.

Scope includes explicit publication-contract registration, narrow transactional marker issuer, committed marker source identity, strict typed decoding, next-transaction capture, full retained bootstrap/delivery, custody-bound source permits and advancement. Marker deliveries stay distinct from revision commits and historical qualification stays false.

Migration022 retains020 bodies under private implementation names, revokes exposed direct EXECUTE, and recreates v1 public wrappers that deny explicitly registered v2 bindings. Legitimate v1 behavior remains exercised, including retries. Public function definitions are intentionally changed; do not interpret “preserved contract” as literal unchanged wrappers.

The native lock test observes the exact active outer custody transaction PID. PostgreSQL can block revocation on that earlier shared fence holder before the inner held source gateway transaction. The test must observe this specific authority holder, commit only after observed blocking, read the exact source-produced receipt, and preserve the opposite-order zero-checkpoint denial. Earlier wrong-PID failures and leaked-transaction evidence are retained.

Review emphasis: source publication downgrade and private EXECUTE bypasses; all real gateway inputs from custody forwarded unchanged; issuance retry identity/mapping/incarnation fencing; open revision transactions across marker consumption; rollback and intervening revisions; complete bootstrap pages and manifest revalidation after delivery writes and after prepare; slot loss/recreation/external advancement; revocation/retry ordering; exact evidence and receipt identity across awaits.

Native worker105/105; Golden Node22 and24 each1908/1908 with builds; SQL concurrency94 OK. Full logs and git-blob identities are in manifest.json. The full candidate tree inventory prevents mistaking a narrow file list for the complete source tree. File names “golden-node22.log” and “native-worker.log” are complete logs, not pass-line excerpts.

Actual fixture SQL ordering is documented precisely in the manifest:001–018 setup,019 continuity,020 incarnation,022 registered integration, then021 distinct proof-only baseline.021 is not required by022 SQL but remains frozen source/evidence.

Outstanding real activation gates: independently admitted source registration and physical custody, crash/rollback exclusion and durable retention contract, provisioning/housekeeping cadence and operational ownership, production authorization, and independent Cursor review. No local files, live Supabase writes, new credentials, deployment or merge occurred.
