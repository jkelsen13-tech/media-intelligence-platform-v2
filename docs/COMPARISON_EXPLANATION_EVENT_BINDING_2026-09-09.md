# Comparison explanation event binding — 9 September 2026

The survivor comparison_public view matched published grouping explanations by article suffix and the exact surface/canonical text prefix, without binding the assertion's event. Two different events can share an article and claim wording. A newer matching explanation from the other event could then win the existing recomputed_at ordering.

The forward correction requires the complete deployed assertion grammar: sc:claim_grouping:<claim event UUID>:<numeric ordinal>:<article UUID>. The event and article must equal the current claim/surface identities. A foreign, malformed or missing own-event explanation yields no explanation; there is no text-only fallback. The existing publication/D4 checks, current/verified retained-source surfaces, approved multi-outlet event, eligible/active source checks, evidence/correction predicates, output fields, temporal values and ordering all remain unchanged.

This binds event identity, not a new immutable worker generation or a full semantic-output receipt. The claim ordinal is syntax-checked; it is not a new persisted claim foreign key. The legacy destructive projection rebuild and broad queue acknowledgement remain separate unresolved collector-cutover requirements. No loss or historical misattribution is asserted from static code alone.

## Evidence and verification

At 2026-09-09T15:21:30.337147Z, the read-only production query found zero currently eligible published explanation matches and zero mismatched-event matches. The defect is established by an isolated PGlite counterexample using the repository's actual prior view and production-shaped foundation. Synthetic rows are confined to CI.

The test proves the old cross-event selection for both anon and authenticated, then checks correct own-event selection, absence without foreign fallback, malformed identifiers, retained history and unchanged access options, migration replay, draft exclusion and source/event retraction. The standard restoration helper includes the forward view so other database tests exercise the corrected foundation.

The filename was generated by Supabase CLI 2.117.0 on the remote preparation branch after inspecting migration-new help. The preparation workflow is excluded from the implementation branch. No local files were created. The production migration version assigned by the connected API and exact SQL readback will be recorded in the PR closeout; do not infer deployment from this document.

The change is CREATE OR REPLACE VIEW only: no retained-row DML, privilege expansion, new endpoint, provider, worker or schedule activation. Required test/build, live deployment, before/after integrity, reader and security-advisor verification remain recorded in the PR. Full backend consolidation and Markets/device qualification remain incomplete.
